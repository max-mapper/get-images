{
  "name": "get-images",
  "subdomain": "images",
  "description": "scrape a page and get an array of all the <a> links to pngs or jpegs",
  "version": "0.0.4",
  "dependencies": {
    "request": "2.12.0",
    "cheerio": "0.10.5"
  },
  "scripts": {
    "start": "server.js"
  },
  "engines": {
    "node": "0.8.x"
  }
}
